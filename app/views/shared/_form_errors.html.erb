<% if object.errors.any? %>
  <div class="form-errors">
    <h2>입력한 내용을 한 번만 다듬어 주세요.</h2>
    <p class="form-errors__meta">아래 항목을 고치면 바로 저장할 수 있어요.</p>
    <ul>
      <% object.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
